#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: is the integer
 *
 * Return: absolute value of i
 */

int _abs(int i)
{
	if (i < 0)
	{
	return (i * -1);
	}
	else
	{
	return (i);
	}
}
